Coses per comentar:

- DONE estructura de classes. (evitar circular dependencies)
  DONE - refracció no funciona glassSpheres (rebound()) https://www.javatpoint.com/upcasting-and-downcasting-in-cpp
- DONE passar doubles i floats a enters per eficiencia (calcul numeric)
  DONE - definir els colors dels objectes en decimals directament DONE
  DONE - reflexció ( de dos formes diferents )
  -DONE tor (solució de eq 4rt grau) DONE (explicar al treball una mica el alogritme (professor mondelo))

DONE - paral·lelogram (no sempre esta ben dibuixat)
DONE -world class

DONE - muliplicar els colors fa els fotos molt fosques. (potser definir la llum per la direccio final del raig)

- DONE implementar objectes no parametritzables (funció distancia i rebot ).
- multithreading.(classe Thread). vertical (preproces imatge per separar els threads),
- justificar numero de rajos per pixel (sample).
  DONE - implementar PaintPixel per un sol pixel. per trobar l'error. (primer identificar on esta el pixel). (posar seed als random)
- compresio a png
- estudi posterior, observacions, conclusions

  .
  .
  .
  .
  .
  .
  .
  .

Que al canviar la dimensio de la imatge reescali la matriu perque es vegi igual pero més petit,
en comptes de retallar la imatge. DONE

glass spheres dont use their custom rebound function in main's PaintPixel func. it's because we are using an array of Objects. DONE

rotate torus (change position DONE)

FUTURE:

Fractal:

- Imatge amb resolució variable.
- quaternions.
- set julia.
